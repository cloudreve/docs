::: details Using Cloudflare, files larger than 100MB cannot be uploaded.

Cloudflare free plan defaults to limiting the size of a single request to upload files to 100MB. You can set chunk upload for the storage policy to bypass this limit, and the chunk size needs to be less than 100MB.

:::
